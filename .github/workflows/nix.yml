name: Nix

on:
  pull_request:
  push:
    branches: [master]

jobs:
  develop:
    runs-on: ubuntu-24.04
    steps:
      - name: Fetch source code
        uses: actions/checkout@v7
        with:
          fetch-depth: 0
          submodules: recursive
      - uses: cachix/install-nix-action@v31
        with:
          github_access_token: ${{ secrets.GITHUB_TOKEN }}
      - uses: cachix/cachix-action@v17
        with:
          name: fcitx5-android
          authToken: "${{ secrets.CACHIX_AUTH_TOKEN }}"
      - name: Build Release APK
        run: |
          nix develop .#noAS --command ./gradlew :app:assembleRelease
          # See C29: the plugins must be built against this fork's application id.
          nix develop .#noAS --command ./gradlew -PmainApplicationId=org.fcitx.fcitx5.android.fx :assembleReleasePlugins
